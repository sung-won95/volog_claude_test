# AI 보컬 코치 웹 애플리케이션 환경 설정 예시
# 이 파일을 .env로 복사하여 사용하세요

# 서버 설정
HOST=0.0.0.0
PORT=8000
DEBUG=true

# 파일 업로드 설정
MAX_FILE_SIZE=104857600  # 100MB
UPLOAD_DIR=uploads
RECORDING_DIR=recordings
STATIC_DIR=web/static

# CORS 설정 (쉼표로 구분)
ALLOWED_ORIGINS=*
